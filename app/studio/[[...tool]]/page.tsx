"use client";

// TODO: import and render NextStudio from 'next-sanity/studio' once sanity.config.ts is set up
export default function StudioPage() {
  return (
    <div className="flex h-screen items-center justify-center bg-obsidian text-linen font-sans">
      <p>Sanity Studio — configure sanity.config.ts to enable</p>
    </div>
  );
}
